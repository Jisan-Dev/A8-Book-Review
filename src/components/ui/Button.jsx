// import PropTypes from 'prop-types';

// const Button = ({ text, bg, textSize }) => {
//   return (
//     <>
//       <button className={`cursor-pointer rounded-lg ${bg} text-white text-[14px] md:text-[18px] font-semibold py-3 md:py-[18px] px-4 md:px-7`}>Sign In</button>
//     </>
//   );
// };

// Button.propTypes = {
//   text: PropTypes.string,
//   bg: PropTypes.string,
//   textSize: PropTypes.string,
// };

// export default Button;
